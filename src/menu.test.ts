import { describe, it } from 'vitest';

// Phase D (#215) deleted src/menu.ts. Phase E (#11) rewrites these tests
// against the Ink overlays (MenuOverlay, TextInputOverlay).
describe.skip('menu (deleted; rewritten in Phase E)', () => {
  it('placeholder', () => {});
});
